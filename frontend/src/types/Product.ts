export type Product = {
    id: string,
    name: string,
    producer: string,
    category: string,
    pricePerBox: number,
    fairMarketValue: number,
    yourPrice: number,
    itemsPerBox: number,
}